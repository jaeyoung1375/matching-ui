name: Front Deploy

on:
  push:
    branches: [develop]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v3

      - name: install
        run: npm install

      - name: build
        run: npm run build

      - name: restart
        run: pm2 restart next-app
